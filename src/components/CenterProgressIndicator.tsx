import { CircularProgress } from '@material-ui/core';
import Center from './Center';

export default function CenterProgressIndicator() {
  return (
    <Center>
      <CircularProgress />
    </Center>
  );
}
